package controller;

import java.awt.event.ActionListener;

/**
 * This interface represents an Image Processing GUI controller, allowing the controller to
 * use functionality as an ActionListener.
 */
public interface ImageProcessingGUIController extends ActionListener, ImageProcessingController {
  // no methods to add.
}
